# Core ML Engine Dependencies (REQUIRED)
onnxruntime>=1.15.0
tiktoken>=0.5.0
numpy>=1.24.0

# Word Processing (REQUIRED for rhymes)
pronouncing>=0.2.0

# Storage (OPTIONAL - only if using Redis)
redis>=4.5.0

# Utilities (OPTIONAL)
python-dotenv>=1.0.0
